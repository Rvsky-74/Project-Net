specVersion: "v1beta"
serviceId: "jsproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jsproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
